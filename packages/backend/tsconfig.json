{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		// NestJS-specific options
		"module": "commonjs",
		"emitDecoratorMetadata": true,

		// Output
		"outDir": "./dist",

		// Paths
		"baseUrl": "./",
		"paths": {
			"@/*": ["src/*"]
		},

		// NestJS requires relaxed strict checks (override base)
		"strict": false,
		"strictNullChecks": false,
		"noImplicitAny": false,
		"strictBindCallApply": false,

		// Monorepo type resolution
		"types": []
	},
	"include": ["src/**/*", "../../config/**/*"],
	"exclude": ["src/**/*.spec.ts", "test/**/*"]
}
