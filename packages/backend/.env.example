# Server
PORT=3000
NODE_ENV=production
FRONTEND_URL=http://localhost:5173

# Database (only required for production, development uses pg-mem in-memory)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=trip_settle
